# 可选：安装本地 AI 模型

不安装模型也能使用规则引擎。安装后，“现场回答追问”会更智能。

1. 安装 Ollama：https://ollama.com/
2. Windows 打开 PowerShell，运行：

```powershell
ollama pull qwen2.5:7b-instruct
```

电脑配置较低可运行：

```powershell
ollama pull llama3.2:3b
```

3. 保持 Ollama 运行，再双击 `启动中文版.bat`。

Ollama 在本机运行，简历不会发送给项目作者。
